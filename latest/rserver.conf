# RStudio Server config for jupyter-rsession-proxy behind the VICE ingress.
# The proxy supplies --www-port and --www-root-path on the rserver command
# line (command-line options take precedence), so neither belongs here.
www-enable-origin-check=0
www-same-site=lax
auth-none=1
auth-cookies-force-secure=0
